<html lang="en-gb">
<header><%- include('_head') -%>

</header>
<nav><%- include('_header') -%></nav>
<main>
    <h1>Posts</h1>
    <ol>
        <% postList.results.forEach(function(post) {%>
            <li>
                <p>
                    <b>
                        <%= post.message %>
                    </b> -<small>
                        <%= format(post.createdAt, "yyyy-MM-dd HH:mm:ss" ) %>
                    </small>
                </p>
                <p><img src="<%=post.imageUrl%>" alt="post image"></p>
                <form method="post" action="/posts/<%=post.id%>/like/">
                    <button type="submit" id="like">Like</button>
                    <button type="submit" id="dislike" formaction="/posts/<%=post.id%>/dislike/">
                        Dislike
                    </button>
                </form>

            </li>
            <% }); %>
    </ol>
    <%if (postList.next) { %>
        <a href="<%= postList.next %>">Next</a>
        <% } %>
            <%if (postList.previous) { %>
                <a href="<%= postList.previous %>">Previous</a>
                <% } %>


</main>
<footer><%- include('_footer') -%></footer>

</html>